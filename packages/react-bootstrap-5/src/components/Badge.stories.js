import React from "react";

const Template = (args) => <div>Badge</div>;

export const Default = Template.bind({});
Default.args = {};

const Story = {
  title: "Components/Badge",
};

export default Story;
